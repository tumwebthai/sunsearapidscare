# ☀️ SunSeaRapidsCare — เว็บบริการรถตู้ VIP พร้อมคนขับ

## วิธีเปิดใช้งานบนคอมพิวเตอร์

### ขั้นตอนที่ 1: ติดตั้ง Node.js
ถ้ายังไม่มี Node.js ให้ดาวน์โหลดจาก https://nodejs.org (แนะนำ LTS version)

### ขั้นตอนที่ 2: ติดตั้ง dependencies
```bash
cd sunsearapidscare
npm install
```

### ขั้นตอนที่ 3: รันเว็บ
```bash
npm run dev
```
เปิดเบราว์เซอร์ไปที่ http://localhost:3000

---

## Deploy ขึ้น Vercel

### วิธีที่ 1: ผ่าน Vercel CLI
```bash
npm i -g vercel
vercel
```

### วิธีที่ 2: ผ่าน GitHub (แนะนำ)
1. Push โค้ดขึ้น GitHub repo
2. ไปที่ https://vercel.com → New Project → Import จาก GitHub
3. Vercel จะ build และ deploy อัตโนมัติ
4. ตั้ง Custom Domain เป็น sunsearapidscare.com

---

## สิ่งที่ต้องแก้ก่อน deploy จริง

| รายการ | ไฟล์ | ตำแหน่ง |
|--------|------|---------|
| เบอร์โทรจริง | `app/page.js` | ค้นหา `0812345678` แล้วเปลี่ยน |
| LINE ID จริง | `app/page.js` | ค้นหา `@sunsearapidscare` แล้วเปลี่ยน |
| WhatsApp จริง | `app/page.js` | ค้นหา `66812345678` แล้วเปลี่ยน |
| Email จริง | `app/page.js` | ค้นหา `info@sunsearapidscare.com` |
| ราคาจริง | `app/page.js` | แก้ใน `fleet` array และ `routes` array |
| รูปรถจริง | `app/page.js` | แทน emoji ด้วย `<Image>` component |

---

## โครงสร้างโปรเจค
```
sunsearapidscare/
├── app/
│   ├── globals.css     # สไตล์ทั่วไป + animations
│   ├── layout.js       # Root layout + SEO meta tags
│   └── page.js         # หน้าหลักทั้งหมด
├── public/             # ใส่รูปรถจริงที่นี่
├── next.config.js
├── package.json
└── README.md
```

## เทคโนโลยี
- **Next.js 14** (App Router)
- **React 18**
- **Google Fonts**: Noto Sans Thai + Outfit
- **Deploy**: Vercel

## สี Theme
- 🟡 Gold: `#C8A35F`
- 🔵 Navy: `#0A1628`
